# You can enter any random string or word but to make it more secure you can generate one
# generated by `node -e "console.log(require('crypto').randomBytes(32).toString('hex'));"`

SESSION_SECRET=

# Spotify Auth
# You will need to login to your Spotify Developer Dashboard to obtain the below info: https://developer.spotify.com/dashboard/applications
# Ensure you've also indicated the Callback URI in the Edit Settings portion of your Spotify App (in the Overview page), this will be your redirect URL below.

SPOTIFY_CLIENT_ID=
SPOTIFY_CLIENT_SECRET=
SPOTIFY_REDIRECT_URL=

# React App and environment (below are default, but feel free to change if desired)

CLIENT_URL=http://localhost:3000
NODE_ENV=development
PORT=8000
